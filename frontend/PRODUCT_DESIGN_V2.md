# TextLoom Frontend - 极简版产品设计

## 一、核心流程（简化版）

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ 上传文档 │ -> │ 配置参数 │ -> │ 生成视频 │ -> │ 查看结果 │ -> │ 任务管理 │
└──────────┘    └──────────┘    └──────────┘    └──────────┘    └──────────┘
    1               2               3               4               5
```

### 用户故事
> 作为用户，我上传一个包含图片 URL 的 Markdown 文档（如 `NanoBananaTripo3D.md`），选择字幕模板，点击"生成视频"按钮，然后等待视频生成完成，最后查看视频和脚本。我还可以在任务列表中查看所有历史任务，点击任务查看详情。

---

## 二、产品约束

### 2.1 功能范围
✅ **包含功能**：
- 单文件上传（仅 Markdown）
- 字幕模板选择
- 视频生成（单任务 = 1 个脚本 + 1 个视频）
- 实时进度监控（轮询）
- 视频播放和下载
- 脚本查看
- **任务列表**（查看所有历史任务）
- **任务详情**（点击任务查看脚本和视频）
- **统计页面**（任务统计和数据可视化）
- **错误展示**（任务失败时显示错误原因）

❌ **不包含功能**：
- 人设管理（写死在后端，前端不展示）
- 多视频生成
- 素材库（批量管理）
- 用户认证

### 2.2 技术约束
- **单任务单视频**：`multi_video_count=1`，`script_style=default`
- **文件类型**：仅支持 `.md`, `.markdown`, `.txt`
- **字幕模板**：hype / minimalist / explosive / vibrant（前端选择，传给后端）
- **轮询间隔**：每 3 秒查询一次任务状态

---

## 三、界面设计（极简版）

### 3.1 应用布局

```
┌─────────────────────────────────────────────────────────┐
│  🎬 TextLoom                                              │
│  [🏠 首页] [📋 任务列表] [📊 统计]           [帮助] [设置]│
└─────────────────────────────────────────────────────────┘
│                                                           │
│                    主内容区域                             │
│                  (根据路由切换页面)                       │
│                                                           │
└─────────────────────────────────────────────────────────┘
```

### 3.2 首页 - 创建任务

```
┌─────────────────────────────────────────────────────────┐
│  🎬 TextLoom - 文档转视频                                 │
│  [🏠 首页] [📋 任务列表] [📊 统计]                        │
└─────────────────────────────────────────────────────────┘

        ┌─────────────────────────────────────┐
        │                                       │
        │     📄 拖拽 Markdown 文件到这里       │
        │         或点击选择文件                │
        │                                       │
        │     支持: .md, .markdown, .txt       │
        │                                       │
        └─────────────────────────────────────┘

        字幕模板: [Hype ▼]  [Minimalist] [Explosive] [Vibrant]

        [🎬 生成视频]

┌───────────────────────────────────────────────────────────┐
│  状态区域（生成中才显示）                                  │
│                                                             │
│  🔄 正在生成视频...                                        │
│  ▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░░░░░░░░░░░░ 45%                      │
│                                                             │
│  当前阶段: 脚本生成中...                                   │
│  预计剩余时间: 5 分钟                                      │
│                                                             │
│  [取消生成]                                                │
└───────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────┐
│  结果区域（生成完成后显示）                                │
│                                                             │
│  ✅ 视频生成成功！                                         │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐ │
│  │                                                       │ │
│  │              🎥 视频播放器                           │ │
│  │                                                       │ │
│  │          [▶️ 播放]  [📥 下载视频]                   │ │
│  │                                                       │ │
│  └─────────────────────────────────────────────────────┘ │
│                                                             │
│  [📝 查看脚本]  [🔄 重新生成]                             │
└───────────────────────────────────────────────────────────┘
```

### 3.2 页面状态流转

```
┌─────────────┐
│  初始状态    │ <- 用户打开页面
│  (Idle)     │
└─────────────┘
      ↓ 用户选择文件
┌─────────────┐
│  文件已选择  │ <- 显示文件名、大小
│  (Ready)    │    启用"生成视频"按钮
└─────────────┘
      ↓ 点击"生成视频"
┌─────────────┐
│  上传中      │ <- 上传进度条
│  (Uploading)│
└─────────────┘
      ↓ 上传完成，创建任务
┌─────────────┐
│  生成中      │ <- 实时进度 + 阶段信息
│ (Processing)│    每 3 秒轮询状态
└─────────────┘
      ↓ 任务完成
┌─────────────┐
│  完成        │ <- 显示视频播放器 + 脚本
│ (Completed) │
└─────────────┘
      ↓ 点击"重新生成"
┌─────────────┐
│  初始状态    │
│  (Idle)     │
└─────────────┘
```

### 3.3 任务列表页面

```
┌─────────────────────────────────────────────────────────┐
│  📋 任务列表                        [🔄 刷新] [➕ 新建任务]│
│  [🏠 首页] [📋 任务列表] [📊 统计]                        │
└─────────────────────────────────────────────────────────┘

筛选: [全部] [进行中] [已完成] [失败]  排序: [创建时间 ↓]

┌───────────────────────────────────────────────────────────┐
│  📝 NanoBananaTripo3D自动化3D工厂         ✅ 已完成        │
│  创建于: 2025-11-26 15:30                                  │
│  时长: 5:30  |  大小: 8.1 MB                               │
│  [查看详情] [下载视频]                                     │
└───────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────┐
│  📝 AI驱动的视频生成技术                🔄 进行中 (45%)    │
│  创建于: 2025-11-26 14:20                                  │
│  当前阶段: 脚本生成中...                                   │
│  [查看详情] [取消任务]                                     │
└───────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────┐
│  📝 产品介绍视频测试                    ❌ 失败            │
│  创建于: 2025-11-26 13:10                                  │
│  错误: 文件下载失败，无法提取媒体素材                     │
│  [查看详情] [重新生成]                                     │
└───────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────┐
│  📝 技术教程视频                        ⏸️ 已取消          │
│  创建于: 2025-11-26 10:45                                  │
│  进度: 25% (用户取消)                                      │
│  [查看详情] [删除]                                         │
└───────────────────────────────────────────────────────────┘

[加载更多...]
```

### 3.4 任务详情页面

```
┌─────────────────────────────────────────────────────────┐
│  ← 返回任务列表                                           │
│  [🏠 首页] [📋 任务列表] [📊 统计]                        │
└─────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────┐
│  📝 NanoBananaTripo3D自动化3D工厂                          │
│  状态: ✅ 已完成  |  创建于: 2025-11-26 15:30              │
├───────────────────────────────────────────────────────────┤
│                                                             │
│  [📹 视频] [📝 脚本] [ℹ️ 信息]                             │
│                                                             │
│  ═════════════════════════════════════════════════════════│
│  视频预览:                                                  │
│  ┌─────────────────────────────────────────────────────┐ │
│  │                                                       │ │
│  │              🎥 视频播放器                           │ │
│  │                                                       │ │
│  │          [▶️ 播放]  [📥 下载视频]                   │ │
│  │                                                       │ │
│  └─────────────────────────────────────────────────────┘ │
│                                                             │
│  视频信息:                                                  │
│  • 时长: 5:30                                              │
│  • 大小: 8.1 MB                                            │
│  • 分辨率: 1080x1920                                       │
│  • 字幕模板: Hype                                          │
│                                                             │
│  [查看完整脚本]  [重新生成]  [删除任务]                   │
└───────────────────────────────────────────────────────────┘
```

**任务详情 - 失败状态展示**：

```
┌───────────────────────────────────────────────────────────┐
│  📝 产品介绍视频测试                                        │
│  状态: ❌ 失败  |  创建于: 2025-11-26 13:10                │
├───────────────────────────────────────────────────────────┤
│                                                             │
│  ⚠️ 任务执行失败                                           │
│                                                             │
│  错误信息:                                                  │
│  ┌─────────────────────────────────────────────────────┐ │
│  │ Markdown文件下载失败: 1/1 个文件失败                  │ │
│  │                                                       │ │
│  │ 失败的URLs:                                           │ │
│  │ • https://example.com/document.md                    │ │
│  │                                                       │ │
│  │ 可能原因:                                             │ │
│  │ - 文件URL无效或已过期                                 │ │
│  │ - 网络连接问题                                        │ │
│  │ - 文件权限不足                                        │ │
│  └─────────────────────────────────────────────────────┘ │
│                                                             │
│  当前阶段: 素材处理 (0%)                                   │
│  失败时间: 2025-11-26 13:10:35                             │
│                                                             │
│  [重新生成]  [编辑配置]  [删除任务]                       │
└───────────────────────────────────────────────────────────┘
```

### 3.5 统计页面

```
┌─────────────────────────────────────────────────────────┐
│  📊 数据统计                                               │
│  [🏠 首页] [📋 任务列表] [📊 统计]                        │
└─────────────────────────────────────────────────────────┘

概览 (最近 30 天)

┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ 总任务数     │ │ 成功率       │ │ 平均处理时间 │
│              │ │              │ │              │
│     23       │ │    78.3%     │ │   12 分钟    │
│              │ │              │ │              │
│ ↑ +5 (本周) │ │ ↑ +2.1%     │ │ ↓ -3 分钟   │
└──────────────┘ └──────────────┘ └──────────────┘

任务状态分布

┌─────────────────────────────────────────────────────────┐
│                                                           │
│  ████████████████████ 已完成 18 (78%)                   │
│  ███ 进行中 2 (9%)                                       │
│  ██ 失败 3 (13%)                                         │
│                                                           │
└─────────────────────────────────────────────────────────┘

近 7 天任务趋势

┌─────────────────────────────────────────────────────────┐
│  任务数                                                   │
│   6 │                                     ●               │
│   5 │                          ●          │               │
│   4 │               ●          │          │               │
│   3 │    ●          │          │          │               │
│   2 │    │     ●    │     ●    │          │               │
│   1 │────┴─────┴────┴─────┴────┴──────────┴──────       │
│     │   周一  周二  周三  周四  周五  周六  周日         │
└─────────────────────────────────────────────────────────┘

失败原因分析

┌─────────────────────────────────────────────────────────┐
│  • 文件下载失败: 40%                                      │
│  • AI 处理超时: 30%                                       │
│  • 视频合成错误: 20%                                      │
│  • 其他错误: 10%                                          │
└─────────────────────────────────────────────────────────┘
```

### 3.6 脚本查看模态框

```
┌───────────────────────────────────────────────────────────┐
│  📝 视频脚本                                      [✕ 关闭] │
├───────────────────────────────────────────────────────────┤
│                                                             │
│  标题: AI驱动的3D建模革命                                  │
│  时长: 5:30                                                 │
│  字数: 820 字                                               │
│                                                             │
│  ─────────────────────────────────────────────────────    │
│                                                             │
│  🎙️ 旁白文本:                                             │
│                                                             │
│  欢迎来到 NanoBanana 的 3D 建模新时代！传统的 3D 建模    │
│  需要几天甚至几周的时间，但现在，通过 Tripo3D 自动化     │
│  工厂，我们可以在短短 5 分钟内完成建模...                 │
│                                                             │
│  （完整旁白文本）                                          │
│                                                             │
│  ─────────────────────────────────────────────────────    │
│                                                             │
│  [📋 复制脚本]  [📥 下载脚本]                             │
└───────────────────────────────────────────────────────────┘
```

---

## 四、组件设计

### 4.1 组件树

```
App
├── Layout
│   ├── Header (顶部导航)
│   │   ├── Logo
│   │   ├── Navigation (首页/任务列表/统计)
│   │   └── Actions (帮助/设置)
│   └── MainContent (主内容区)
│
├── Pages (路由页面)
│   ├── HomePage (首页 - 创建任务)
│   │   ├── FileUploader (文件上传)
│   │   │   ├── DropZone (拖拽区域)
│   │   │   └── FileInfo (文件信息)
│   │   ├── SubtitleTemplateSelector (字幕模板选择)
│   │   ├── GenerateButton (生成按钮)
│   │   ├── ProgressPanel (进度面板) - 条件渲染
│   │   │   ├── ProgressBar (进度条)
│   │   │   ├── StageInfo (阶段信息)
│   │   │   └── CancelButton (取消按钮)
│   │   └── ResultPanel (结果面板) - 条件渲染
│   │       ├── VideoPlayer (视频播放器)
│   │       └── ActionButtons (操作按钮)
│   │
│   ├── TaskListPage (任务列表)
│   │   ├── TaskFilters (筛选器)
│   │   ├── TaskSorter (排序)
│   │   └── TaskList (任务列表)
│   │       └── TaskCard (任务卡片) × N
│   │           ├── StatusBadge (状态徽章)
│   │           ├── TaskInfo (任务信息)
│   │           └── ActionButtons (操作按钮)
│   │
│   ├── TaskDetailPage (任务详情)
│   │   ├── BackButton (返回按钮)
│   │   ├── TaskHeader (任务头部)
│   │   ├── TabBar (Tab 切换: 视频/脚本/信息)
│   │   ├── VideoTab (视频 Tab)
│   │   │   ├── VideoPlayer (视频播放器)
│   │   │   └── VideoInfo (视频信息)
│   │   ├── ScriptTab (脚本 Tab)
│   │   │   └── ScriptContent (脚本内容)
│   │   ├── InfoTab (信息 Tab)
│   │   │   └── TaskMetadata (任务元数据)
│   │   └── ErrorDisplay (错误展示) - 失败状态
│   │
│   └── StatsPage (统计页面)
│       ├── StatsOverview (统计概览)
│       │   ├── StatCard (统计卡片) × 3
│       ├── StatusChart (状态分布图)
│       ├── TrendChart (趋势图表)
│       └── ErrorAnalysis (错误分析)
│
├── Shared Components (共享组件)
│   ├── ScriptModal (脚本模态框)
│   ├── ConfirmDialog (确认对话框)
│   └── Toast (通知提示)
│
└── UI Components (shadcn/ui)
    ├── Button
    ├── Card
    ├── Dialog
    ├── Progress
    ├── Badge
    ├── Tabs
    └── ...
```

### 4.2 新增组件详细设计

#### TaskCard 组件（任务列表卡片）

```typescript
interface TaskCardProps {
  task: Task
  onView: (id: string) => void
  onCancel?: (id: string) => void
  onDelete?: (id: string) => void
  onRetry?: (id: string) => void
}

// UI 结构
<Card>
  <CardHeader>
    <div className="flex items-center justify-between">
      <h3>{task.title}</h3>
      <StatusBadge status={task.status} />
    </div>
    <p className="text-sm text-muted-foreground">
      创建于: {formatDate(task.created_at)}
    </p>
  </CardHeader>

  <CardContent>
    {/* 根据状态显示不同内容 */}
    {task.status === 'processing' && (
      <>
        <Progress value={task.progress} />
        <p>当前阶段: {task.current_stage}</p>
      </>
    )}

    {task.status === 'completed' && (
      <p>时长: {task.duration} | 大小: {task.size}</p>
    )}

    {task.status === 'failed' && (
      <Alert variant="destructive">
        <AlertCircle className="h-4 w-4" />
        <AlertDescription>{task.error_message}</AlertDescription>
      </Alert>
    )}
  </CardContent>

  <CardFooter>
    <Button onClick={() => onView(task.id)}>查看详情</Button>
    {/* 根据状态显示不同操作按钮 */}
  </CardFooter>
</Card>
```

#### ErrorDisplay 组件（错误展示）

```typescript
interface ErrorDisplayProps {
  error: {
    message: string
    stage?: string
    failedUrls?: string[]
    timestamp?: string
  }
  onRetry?: () => void
  onEdit?: () => void
  onDelete?: () => void
}

// UI 结构
<Alert variant="destructive" className="mb-6">
  <AlertCircle className="h-4 w-4" />
  <AlertTitle>⚠️ 任务执行失败</AlertTitle>
  <AlertDescription>
    <div className="mt-4 space-y-3">
      <div>
        <h4 className="font-medium mb-2">错误信息:</h4>
        <div className="bg-destructive/10 p-3 rounded-md">
          <p className="text-sm">{error.message}</p>

          {error.failedUrls && (
            <div className="mt-2">
              <p className="text-sm font-medium">失败的URLs:</p>
              <ul className="list-disc list-inside text-sm">
                {error.failedUrls.map(url => (
                  <li key={url}>{url}</li>
                ))}
              </ul>
            </div>
          )}

          <div className="mt-2 text-sm">
            <p className="font-medium">可能原因:</p>
            <ul className="list-disc list-inside">
              <li>文件URL无效或已过期</li>
              <li>网络连接问题</li>
              <li>文件权限不足</li>
            </ul>
          </div>
        </div>
      </div>

      {error.stage && (
        <p className="text-sm">当前阶段: {error.stage}</p>
      )}

      {error.timestamp && (
        <p className="text-sm">失败时间: {formatDateTime(error.timestamp)}</p>
      )}
    </div>
  </AlertDescription>
</Alert>

<div className="flex gap-3">
  {onRetry && <Button onClick={onRetry}>重新生成</Button>}
  {onEdit && <Button variant="outline" onClick={onEdit}>编辑配置</Button>}
  {onDelete && <Button variant="destructive" onClick={onDelete}>删除任务</Button>}
</div>
```

#### StatsCard 组件（统计卡片）

```typescript
interface StatsCardProps {
  title: string
  value: string | number
  change?: {
    value: number
    label: string
  }
  icon?: React.ReactNode
}

<Card>
  <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2">
    <CardTitle className="text-sm font-medium">{title}</CardTitle>
    {icon}
  </CardHeader>
  <CardContent>
    <div className="text-2xl font-bold">{value}</div>
    {change && (
      <p className={cn(
        "text-xs",
        change.value > 0 ? "text-green-600" : "text-red-600"
      )}>
        {change.value > 0 ? '↑' : '↓'} {Math.abs(change.value)} {change.label}
      </p>
    )}
  </CardContent>
</Card>
```

### 4.3 核心组件详细设计

#### FileUploader 组件

```typescript
interface FileUploaderProps {
  onFileSelect: (file: File) => void
  acceptedTypes: string[]
  maxSize: number
}

// 状态
const [file, setFile] = useState<File | null>(null)
const [isDragging, setIsDragging] = useState(false)

// 交互
- 拖拽上传 (Drag & Drop)
- 点击选择文件
- 文件验证（类型、大小）
- 显示文件名、大小
```

#### SubtitleTemplateSelector 组件

```typescript
interface Template {
  id: string
  name: string
  description: string
  preview?: string
}

const templates: Template[] = [
  { id: 'hype', name: 'Hype', description: '动感激情风格' },
  { id: 'minimalist', name: 'Minimalist', description: '极简风格' },
  { id: 'explosive', name: 'Explosive', description: '爆炸式风格' },
  { id: 'vibrant', name: 'Vibrant', description: '活力四射风格' }
]

// UI: 单选按钮组或下拉菜单
```

#### ProgressPanel 组件

```typescript
interface ProgressPanelProps {
  progress: number       // 0-100
  stage: string          // 当前阶段
  message: string        // 阶段说明
  onCancel: () => void
}

// 实时更新
- 每 3 秒轮询 GET /api/tasks/{id}/status
- 解析 progress, current_stage, stage_message
- 显示预估剩余时间
```

#### VideoPlayer 组件

```typescript
interface VideoPlayerProps {
  videoUrl: string
  thumbnailUrl?: string
  onDownload: () => void
}

// 使用 react-player
import ReactPlayer from 'react-player'

<ReactPlayer
  url={videoUrl}
  controls
  width="100%"
  height="auto"
/>
```

#### ScriptModal 组件

```typescript
interface ScriptModalProps {
  isOpen: boolean
  onClose: () => void
  script: {
    title: string
    narration: string
    duration: number
    word_count: number
  }
}

// 使用 shadcn/ui Dialog
- Markdown 渲染旁白文本
- 复制到剪贴板功能
- 下载为 .txt 文件
```

---

## 五、API 集成

### 5.1 API 流程

```
1. 上传文件
   POST /api/tasks/attachments/upload
   Body: FormData (file: File)
   Response: { items: [{ url, filename, media_type }] }

2. 创建任务
   POST /api/tasks/create-video-task
   Body: FormData {
     title: string
     mode: "multi_scene"
     script_style: "default"
     multi_video_count: 1
     media_urls: string[] (从步骤1获取的 URL)
     subtitle_template: "hype" | "minimalist" | ...
   }
   Response: { id, status, ... }

3. 轮询状态
   GET /api/tasks/{id}/status
   Response: {
     status, progress, current_stage, stage_message,
     multi_video_info: {
       completed_videos: [{ video_url, thumbnail_url, ... }]
     }
   }

4. 获取详情
   GET /api/tasks/{id}
   Response: {
     script_content: { title, narration, duration, word_count },
     ...
   }
```

### 5.2 API 客户端封装

```typescript
// lib/api.ts
import axios from 'axios'

const api = axios.create({
  baseURL: '/api',
  timeout: 60000,
  headers: {
    'X-API-Key': 'bda38eb13ae2eaffd1cbbfb050f288e1de3ed5985873b157795d03a5f675959c'
  }
})

export const videoApi = {
  // 上传文件
  uploadFile: async (file: File) => {
    const formData = new FormData()
    formData.append('files', file)
    return api.post<UploadResponse>('/tasks/attachments/upload', formData, {
      headers: { 'Content-Type': 'multipart/form-data' }
    })
  },

  // 创建任务
  createTask: async (params: CreateTaskParams) => {
    const formData = new FormData()
    formData.append('title', params.title)
    formData.append('mode', 'multi_scene')
    formData.append('script_style', 'default')
    formData.append('multi_video_count', '1')
    params.mediaUrls.forEach(url => formData.append('media_urls', url))
    if (params.subtitleTemplate) {
      formData.append('subtitle_template', params.subtitleTemplate)
    }
    return api.post<Task>('/tasks/create-video-task', formData)
  },

  // 获取任务状态
  getTaskStatus: async (taskId: string) => {
    return api.get<TaskStatus>(`/tasks/${taskId}/status`)
  },

  // 获取任务详情
  getTaskDetail: async (taskId: string) => {
    return api.get<TaskDetail>(`/tasks/${taskId}`)
  },

  // 取消任务
  cancelTask: async (taskId: string) => {
    return api.post(`/tasks/${taskId}/cancel`)
  }
}
```

---

## 六、状态管理

### 6.1 使用 Zustand

```typescript
// stores/videoStore.ts
import { create } from 'zustand'

interface VideoState {
  // 文件
  file: File | null
  setFile: (file: File | null) => void

  // 字幕模板
  subtitleTemplate: string
  setSubtitleTemplate: (template: string) => void

  // 任务
  taskId: string | null
  taskStatus: 'idle' | 'uploading' | 'processing' | 'completed' | 'failed'
  progress: number
  stage: string
  stageMessage: string
  errorMessage: string | null

  // 视频
  videoUrl: string | null
  thumbnailUrl: string | null
  scriptContent: ScriptContent | null

  // Actions
  startUpload: () => void
  uploadSuccess: (taskId: string) => void
  uploadFailed: (error: string) => void
  updateProgress: (data: ProgressData) => void
  setCompleted: (data: CompletedData) => void
  reset: () => void
}

export const useVideoStore = create<VideoState>((set) => ({
  file: null,
  setFile: (file) => set({ file }),

  subtitleTemplate: 'hype',
  setSubtitleTemplate: (template) => set({ subtitleTemplate: template }),

  taskId: null,
  taskStatus: 'idle',
  progress: 0,
  stage: '',
  stageMessage: '',
  errorMessage: null,

  videoUrl: null,
  thumbnailUrl: null,
  scriptContent: null,

  startUpload: () => set({ taskStatus: 'uploading', errorMessage: null }),
  uploadSuccess: (taskId) => set({ taskId, taskStatus: 'processing', progress: 0 }),
  uploadFailed: (error) => set({ taskStatus: 'failed', errorMessage: error }),

  updateProgress: (data) => set({
    progress: data.progress,
    stage: data.stage,
    stageMessage: data.stageMessage
  }),

  setCompleted: (data) => set({
    taskStatus: 'completed',
    progress: 100,
    videoUrl: data.videoUrl,
    thumbnailUrl: data.thumbnailUrl,
    scriptContent: data.scriptContent
  }),

  reset: () => set({
    file: null,
    taskId: null,
    taskStatus: 'idle',
    progress: 0,
    stage: '',
    stageMessage: '',
    errorMessage: null,
    videoUrl: null,
    thumbnailUrl: null,
    scriptContent: null
  })
}))
```

### 6.2 路由配置

```typescript
// App.tsx
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom'
import { Layout } from '@/components/layout/Layout'
import { HomePage } from '@/pages/HomePage'
import { TaskListPage } from '@/pages/TaskListPage'
import { TaskDetailPage } from '@/pages/TaskDetailPage'
import { StatsPage } from '@/pages/StatsPage'

export function App() {
  return (
    <BrowserRouter>
      <Layout>
        <Routes>
          <Route path="/" element={<HomePage />} />
          <Route path="/tasks" element={<TaskListPage />} />
          <Route path="/tasks/:taskId" element={<TaskDetailPage />} />
          <Route path="/stats" element={<StatsPage />} />
          <Route path="*" element={<Navigate to="/" replace />} />
        </Routes>
      </Layout>
    </BrowserRouter>
  )
}
```

**路由说明**：
- `/` - 首页（创建任务）
- `/tasks` - 任务列表
- `/tasks/:taskId` - 任务详情
- `/stats` - 统计页面

---

## 七、核心逻辑实现

### 7.1 主页面逻辑

```typescript
// pages/Home.tsx
import { useVideoStore } from '@/stores/videoStore'
import { videoApi } from '@/lib/api'
import { useEffect, useRef } from 'react'

export function HomePage() {
  const store = useVideoStore()
  const pollingIntervalRef = useRef<NodeJS.Timeout | null>(null)

  // 生成视频
  const handleGenerate = async () => {
    if (!store.file) return

    try {
      // 1. 上传文件
      store.startUpload()
      const uploadRes = await videoApi.uploadFile(store.file)

      if (!uploadRes.data.items || uploadRes.data.items.length === 0) {
        throw new Error('文件上传失败')
      }

      const mediaUrls = uploadRes.data.items
        .filter(item => item.success && item.url)
        .map(item => item.url)

      // 2. 创建任务
      const taskRes = await videoApi.createTask({
        title: store.file.name.replace(/\.[^/.]+$/, ''), // 去掉扩展名
        mediaUrls,
        subtitleTemplate: store.subtitleTemplate
      })

      store.uploadSuccess(taskRes.data.id)

      // 3. 开始轮询
      startPolling(taskRes.data.id)

    } catch (error) {
      store.uploadFailed(error.message)
    }
  }

  // 轮询任务状态
  const startPolling = (taskId: string) => {
    if (pollingIntervalRef.current) {
      clearInterval(pollingIntervalRef.current)
    }

    pollingIntervalRef.current = setInterval(async () => {
      try {
        const statusRes = await videoApi.getTaskStatus(taskId)
        const status = statusRes.data

        // 更新进度
        store.updateProgress({
          progress: status.progress,
          stage: status.current_stage || '',
          stageMessage: status.stage_message || ''
        })

        // 检查是否完成
        if (status.status === 'completed') {
          stopPolling()

          // 获取详情（视频 URL + 脚本）
          const detailRes = await videoApi.getTaskDetail(taskId)
          const detail = detailRes.data

          const completedVideo = status.multi_video_info?.completed_videos?.[0]

          store.setCompleted({
            videoUrl: completedVideo?.video_url || '',
            thumbnailUrl: completedVideo?.thumbnail_url || '',
            scriptContent: detail.script_content
          })

        } else if (status.status === 'failed') {
          stopPolling()
          store.uploadFailed(status.error_message || '视频生成失败')
        }

      } catch (error) {
        console.error('轮询状态失败:', error)
      }
    }, 3000) // 每 3 秒轮询一次
  }

  const stopPolling = () => {
    if (pollingIntervalRef.current) {
      clearInterval(pollingIntervalRef.current)
      pollingIntervalRef.current = null
    }
  }

  // 取消任务
  const handleCancel = async () => {
    if (!store.taskId) return

    try {
      await videoApi.cancelTask(store.taskId)
      stopPolling()
      store.reset()
    } catch (error) {
      console.error('取消任务失败:', error)
    }
  }

  // 清理
  useEffect(() => {
    return () => {
      stopPolling()
    }
  }, [])

  return (
    <div className="container mx-auto max-w-4xl p-6">
      <Header />

      {/* 初始状态 or 文件已选择 */}
      {(store.taskStatus === 'idle' || store.taskStatus === 'failed') && (
        <>
          <FileUploader
            onFileSelect={store.setFile}
            selectedFile={store.file}
          />

          <SubtitleTemplateSelector
            value={store.subtitleTemplate}
            onChange={store.setSubtitleTemplate}
          />

          <Button
            onClick={handleGenerate}
            disabled={!store.file}
            size="lg"
            className="w-full"
          >
            🎬 生成视频
          </Button>

          {store.taskStatus === 'failed' && (
            <Alert variant="destructive">
              <AlertCircle className="h-4 w-4" />
              <AlertTitle>生成失败</AlertTitle>
              <AlertDescription>{store.errorMessage}</AlertDescription>
            </Alert>
          )}
        </>
      )}

      {/* 上传中 */}
      {store.taskStatus === 'uploading' && (
        <div className="text-center py-12">
          <Loader2 className="h-12 w-12 animate-spin mx-auto" />
          <p className="mt-4 text-muted-foreground">正在上传文件...</p>
        </div>
      )}

      {/* 生成中 */}
      {store.taskStatus === 'processing' && (
        <ProgressPanel
          progress={store.progress}
          stage={store.stage}
          stageMessage={store.stageMessage}
          onCancel={handleCancel}
        />
      )}

      {/* 完成 */}
      {store.taskStatus === 'completed' && (
        <ResultPanel
          videoUrl={store.videoUrl!}
          thumbnailUrl={store.thumbnailUrl}
          scriptContent={store.scriptContent}
          onReset={store.reset}
        />
      )}
    </div>
  )
}
```

---

## 八、UI 组件实现

### 8.1 FileUploader 组件

```typescript
// components/FileUploader.tsx
import { useCallback } from 'react'
import { useDropzone } from 'react-dropzone'
import { Upload, File, X } from 'lucide-react'
import { Card } from '@/components/ui/card'
import { Button } from '@/components/ui/button'

interface FileUploaderProps {
  onFileSelect: (file: File | null) => void
  selectedFile: File | null
}

export function FileUploader({ onFileSelect, selectedFile }: FileUploaderProps) {
  const onDrop = useCallback((acceptedFiles: File[]) => {
    if (acceptedFiles.length > 0) {
      onFileSelect(acceptedFiles[0])
    }
  }, [onFileSelect])

  const { getRootProps, getInputProps, isDragActive } = useDropzone({
    onDrop,
    accept: {
      'text/markdown': ['.md', '.markdown'],
      'text/plain': ['.txt']
    },
    maxFiles: 1,
    maxSize: 10 * 1024 * 1024 // 10MB
  })

  if (selectedFile) {
    return (
      <Card className="p-6 mb-6">
        <div className="flex items-center justify-between">
          <div className="flex items-center gap-3">
            <File className="h-8 w-8 text-primary" />
            <div>
              <p className="font-medium">{selectedFile.name}</p>
              <p className="text-sm text-muted-foreground">
                {(selectedFile.size / 1024).toFixed(2)} KB
              </p>
            </div>
          </div>
          <Button
            variant="ghost"
            size="icon"
            onClick={() => onFileSelect(null)}
          >
            <X className="h-4 w-4" />
          </Button>
        </div>
      </Card>
    )
  }

  return (
    <Card
      {...getRootProps()}
      className={cn(
        "p-12 mb-6 border-2 border-dashed cursor-pointer transition-colors",
        isDragActive ? "border-primary bg-primary/5" : "border-muted-foreground/25"
      )}
    >
      <input {...getInputProps()} />
      <div className="text-center">
        <Upload className="h-12 w-12 mx-auto mb-4 text-muted-foreground" />
        <p className="text-lg font-medium mb-2">
          {isDragActive ? "放开以上传文件" : "拖拽 Markdown 文件到这里"}
        </p>
        <p className="text-sm text-muted-foreground mb-4">
          或点击选择文件
        </p>
        <p className="text-xs text-muted-foreground">
          支持: .md, .markdown, .txt（最大 10MB）
        </p>
      </div>
    </Card>
  )
}
```

### 8.2 SubtitleTemplateSelector 组件

```typescript
// components/SubtitleTemplateSelector.tsx
import { Card } from '@/components/ui/card'
import { Label } from '@/components/ui/label'
import { RadioGroup, RadioGroupItem } from '@/components/ui/radio-group'

const templates = [
  { id: 'hype', name: 'Hype', description: '动感激情，适合产品宣传' },
  { id: 'minimalist', name: 'Minimalist', description: '极简风格，清晰易读' },
  { id: 'explosive', name: 'Explosive', description: '爆炸式效果，强烈冲击' },
  { id: 'vibrant', name: 'Vibrant', description: '活力四射，色彩丰富' }
]

interface SubtitleTemplateSelectorProps {
  value: string
  onChange: (value: string) => void
}

export function SubtitleTemplateSelector({ value, onChange }: SubtitleTemplateSelectorProps) {
  return (
    <Card className="p-6 mb-6">
      <h3 className="text-lg font-semibold mb-4">字幕模板</h3>
      <RadioGroup value={value} onValueChange={onChange}>
        <div className="grid grid-cols-2 gap-4">
          {templates.map(template => (
            <div key={template.id} className="flex items-start space-x-3">
              <RadioGroupItem value={template.id} id={template.id} />
              <Label htmlFor={template.id} className="cursor-pointer">
                <div className="font-medium">{template.name}</div>
                <div className="text-sm text-muted-foreground">
                  {template.description}
                </div>
              </Label>
            </div>
          ))}
        </div>
      </RadioGroup>
    </Card>
  )
}
```

### 8.3 ProgressPanel 组件

```typescript
// components/ProgressPanel.tsx
import { Card } from '@/components/ui/card'
import { Progress } from '@/components/ui/progress'
import { Button } from '@/components/ui/button'
import { Loader2 } from 'lucide-react'

interface ProgressPanelProps {
  progress: number
  stage: string
  stageMessage: string
  onCancel: () => void
}

export function ProgressPanel({ progress, stage, stageMessage, onCancel }: ProgressPanelProps) {
  // 预估剩余时间（简单计算）
  const estimatedTime = Math.max(0, Math.ceil((100 - progress) / 100 * 10)) // 假设总共10分钟

  return (
    <Card className="p-8">
      <div className="text-center mb-6">
        <Loader2 className="h-12 w-12 animate-spin mx-auto mb-4 text-primary" />
        <h3 className="text-xl font-semibold mb-2">🔄 正在生成视频...</h3>
      </div>

      <Progress value={progress} className="mb-4 h-3" />

      <div className="text-center mb-6">
        <p className="text-2xl font-bold text-primary mb-2">{progress.toFixed(1)}%</p>
        {stage && (
          <p className="text-sm text-muted-foreground mb-1">
            当前阶段: {stage}
          </p>
        )}
        {stageMessage && (
          <p className="text-xs text-muted-foreground mb-2">
            {stageMessage}
          </p>
        )}
        {estimatedTime > 0 && (
          <p className="text-xs text-muted-foreground">
            预计剩余时间: 约 {estimatedTime} 分钟
          </p>
        )}
      </div>

      <div className="flex justify-center">
        <Button variant="outline" onClick={onCancel}>
          取消生成
        </Button>
      </div>
    </Card>
  )
}
```

### 8.4 ResultPanel 组件

```typescript
// components/ResultPanel.tsx
import { useState } from 'react'
import { Card } from '@/components/ui/card'
import { Button } from '@/components/ui/button'
import { CheckCircle, Download, FileText, RefreshCw } from 'lucide-react'
import ReactPlayer from 'react-player'
import { ScriptModal } from './ScriptModal'

interface ResultPanelProps {
  videoUrl: string
  thumbnailUrl?: string | null
  scriptContent: any
  onReset: () => void
}

export function ResultPanel({ videoUrl, thumbnailUrl, scriptContent, onReset }: ResultPanelProps) {
  const [isScriptOpen, setIsScriptOpen] = useState(false)

  const handleDownload = () => {
    window.open(videoUrl, '_blank')
  }

  return (
    <>
      <Card className="p-8">
        <div className="text-center mb-6">
          <CheckCircle className="h-12 w-12 mx-auto mb-4 text-green-500" />
          <h3 className="text-xl font-semibold text-green-600">✅ 视频生成成功！</h3>
        </div>

        <div className="aspect-video bg-black rounded-lg overflow-hidden mb-6">
          <ReactPlayer
            url={videoUrl}
            controls
            width="100%"
            height="100%"
            light={thumbnailUrl || false}
            playing={false}
          />
        </div>

        <div className="flex gap-3 justify-center">
          <Button onClick={handleDownload} size="lg">
            <Download className="mr-2 h-4 w-4" />
            下载视频
          </Button>
          {scriptContent && (
            <Button variant="outline" size="lg" onClick={() => setIsScriptOpen(true)}>
              <FileText className="mr-2 h-4 w-4" />
              查看脚本
            </Button>
          )}
          <Button variant="outline" size="lg" onClick={onReset}>
            <RefreshCw className="mr-2 h-4 w-4" />
            重新生成
          </Button>
        </div>
      </Card>

      {scriptContent && (
        <ScriptModal
          isOpen={isScriptOpen}
          onClose={() => setIsScriptOpen(false)}
          script={scriptContent}
        />
      )}
    </>
  )
}
```

### 8.5 ScriptModal 组件

```typescript
// components/ScriptModal.tsx
import { Dialog, DialogContent, DialogHeader, DialogTitle } from '@/components/ui/dialog'
import { Button } from '@/components/ui/button'
import { Copy, Download } from 'lucide-react'
import { useToast } from '@/hooks/use-toast'

interface ScriptModalProps {
  isOpen: boolean
  onClose: () => void
  script: {
    title?: string
    narration?: string
    duration?: number
    word_count?: number
  }
}

export function ScriptModal({ isOpen, onClose, script }: ScriptModalProps) {
  const { toast } = useToast()

  const handleCopy = () => {
    navigator.clipboard.writeText(script.narration || '')
    toast({
      title: "已复制",
      description: "脚本内容已复制到剪贴板"
    })
  }

  const handleDownload = () => {
    const blob = new Blob([script.narration || ''], { type: 'text/plain' })
    const url = URL.createObjectURL(blob)
    const a = document.createElement('a')
    a.href = url
    a.download = `${script.title || '脚本'}.txt`
    a.click()
    URL.revokeObjectURL(url)
  }

  return (
    <Dialog open={isOpen} onOpenChange={onClose}>
      <DialogContent className="max-w-3xl max-h-[80vh] overflow-y-auto">
        <DialogHeader>
          <DialogTitle>📝 视频脚本</DialogTitle>
        </DialogHeader>

        <div className="space-y-4">
          {script.title && (
            <div>
              <h4 className="text-sm font-medium text-muted-foreground mb-1">标题</h4>
              <p className="text-lg font-semibold">{script.title}</p>
            </div>
          )}

          <div className="flex gap-4 text-sm text-muted-foreground">
            {script.duration && (
              <span>⏱️ 时长: {script.duration} 秒</span>
            )}
            {script.word_count && (
              <span>📊 字数: {script.word_count} 字</span>
            )}
          </div>

          <hr />

          <div>
            <h4 className="text-sm font-medium text-muted-foreground mb-2">🎙️ 旁白文本</h4>
            <div className="bg-muted p-4 rounded-lg whitespace-pre-wrap font-mono text-sm">
              {script.narration}
            </div>
          </div>

          <div className="flex gap-2">
            <Button onClick={handleCopy} variant="outline">
              <Copy className="mr-2 h-4 w-4" />
              复制脚本
            </Button>
            <Button onClick={handleDownload} variant="outline">
              <Download className="mr-2 h-4 w-4" />
              下载脚本
            </Button>
          </div>
        </div>
      </DialogContent>
    </Dialog>
  )
}
```

---

## 九、开发计划

### Phase 1: 基础架构（2-3 天）
1. ✅ 项目初始化（Vite + React + TypeScript + shadcn/ui + Tailwind）
2. ✅ 路由配置（React Router）
3. ✅ 布局组件（Header + Navigation）
4. ✅ API 客户端封装
5. ✅ Zustand 状态管理
6. ✅ 类型定义

### Phase 2: 首页 - 创建任务（3-4 天）
7. ✅ 文件上传组件（拖拽 + 选择）
8. ✅ 字幕模板选择器
9. ✅ 生成按钮和表单验证
10. ✅ 进度面板组件
11. ✅ 轮询逻辑实现
12. ✅ 结果展示组件
13. ✅ 视频播放器集成
14. ✅ 脚本模态框

### Phase 3: 任务列表与详情（3-4 天）
15. ✅ 任务列表页面
16. ✅ 任务卡片组件
17. ✅ 任务筛选和排序
18. ✅ 任务详情页面
19. ✅ Tab 切换（视频/脚本/信息）
20. ✅ 错误展示组件
21. ✅ 任务操作（取消/删除/重试）

### Phase 4: 统计页面（2-3 天）
22. ✅ 统计概览卡片
23. ✅ 任务状态分布图
24. ✅ 趋势图表集成（recharts）
25. ✅ 失败原因分析

### Phase 5: 优化和测试（2-3 天）
26. ✅ UI 细节打磨
27. ✅ 响应式布局
28. ✅ 错误边界处理
29. ✅ 加载状态优化
30. ✅ 性能优化
31. ✅ E2E 测试

**总计**: 约 2.5-3 周完成

---

## 十、技术栈总结

```json
{
  "核心": {
    "框架": "React 18",
    "语言": "TypeScript 5+",
    "构建工具": "Vite 5+"
  },
  "UI": {
    "组件库": "shadcn/ui",
    "样式": "Tailwind CSS",
    "图标": "lucide-react"
  },
  "状态": {
    "全局状态": "zustand",
    "服务端状态": "@tanstack/react-query"
  },
  "表单": {
    "表单库": "react-hook-form",
    "验证": "zod"
  },
  "工具": {
    "HTTP": "axios",
    "文件上传": "react-dropzone",
    "视频播放": "react-player",
    "路由": "react-router-dom (可选)",
    "日期": "date-fns"
  }
}
```

---

## 十一、关键技术点

### 11.1 后端字幕模板支持

**需要确认**: 后端是否支持在创建任务时传入 `subtitle_template` 参数？

如果不支持，有两种方案：
1. **方案 A**: 在后端添加 `subtitle_template` 参数支持（推荐）
2. **方案 B**: 前端仅作为 UI 选择，实际使用默认模板

### 11.2 单任务单视频约束

在创建任务时强制：
- `multi_video_count=1`
- `script_style="default"`
- `mode="multi_scene"`

### 11.3 轮询优化

- 使用 `setInterval` 每 3 秒轮询
- 任务完成或失败后立即停止轮询
- 组件卸载时清理定时器
- 可选：使用 WebSocket 替代轮询（如果后端支持）

### 11.4 错误处理

- 文件上传失败
- 任务创建失败
- 任务执行失败
- 网络错误
- 使用 `react-hot-toast` 或 `shadcn/ui toast` 显示错误

---

## 附录：完整类型定义

```typescript
// lib/types.ts

export interface UploadResponse {
  items: UploadItem[]
  stats: {
    markdown_count: number
    image_count: number
    video_count: number
  }
  warnings?: string[]
}

export interface UploadItem {
  filename: string
  url: string
  object_key: string
  media_type: 'markdown' | 'image' | 'video' | 'unknown'
  size: number
  success: boolean
  error?: string
}

export interface CreateTaskParams {
  title: string
  mediaUrls: string[]
  subtitleTemplate?: string
}

export interface Task {
  id: string
  title: string
  status: TaskStatusType
  progress: number
  created_at: string
  updated_at: string
}

export type TaskStatusType = 'pending' | 'processing' | 'completed' | 'failed' | 'cancelled'

export interface TaskStatus extends Task {
  current_stage?: string
  stage_message?: string
  error_message?: string
  multi_video_info?: {
    total_videos: number
    completed_count: number
    failed_count: number
    processing_count: number
    completed_videos: CompletedVideo[]
    failed_videos: FailedVideo[]
    processing_videos: ProcessingVideo[]
  }
}

export interface CompletedVideo {
  sub_task_id: string
  script_style: string
  video_url: string
  thumbnail_url?: string
  video_duration?: number
}

export interface FailedVideo {
  sub_task_id: string
  script_style: string
  error_message: string
}

export interface ProcessingVideo {
  sub_task_id: string
  script_style: string
  progress: number
}

export interface TaskDetail extends Task {
  description?: string
  mode: string
  script_style: string
  script_content?: ScriptContent
  media_items_count: number
}

export interface ScriptContent {
  title?: string
  description?: string
  narration?: string
  full_text?: string
  tags?: string[]
  estimated_duration?: number
  duration?: number
  word_count?: number
  material_count?: number
}

export interface ProgressData {
  progress: number
  stage: string
  stageMessage: string
}

export interface CompletedData {
  videoUrl: string
  thumbnailUrl?: string | null
  scriptContent: ScriptContent | null
}
```
